swagger: '2.0'
info:
  title: Timelime API
  description: Timelime API
  version: 1.0.0
host: gateway-uavkhjofda-nw.a.run.app
schemes:
  - https
produces:
  - application/json

paths:
  /quentinoo:
    get:
      summary: Get Quentin
      operationId: quentinoo
      responses:
       '200':
          description: OK
          schema:
            type: string
      x-google-backend:
        address: https://timelime-backend-uavkhjofda-nw.a.run.app/quentin
        protocol: h2

securityDefinitions:
  firebase:
    authorizationUrl: ""
    flow: "implicit"
    type: "oauth2"
    x-google-issuer: "https://securetoken.google.com/timelime-dev"
    x-google-jwks_uri: "https://www.googleapis.com/service_accounts/v1/metadata/x509/securetoken@system.gserviceaccount.com"
    x-google-audiences: "timelime-dev"

security:
  - firebase: []