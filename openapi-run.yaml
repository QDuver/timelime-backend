swagger: '2.0'
info:
  title: Timelime API
  description: Timelime API
  version: 1.0.0
host: gateway-uavkhjofda-nw.a.run.app
schemes:
  - https
produces:
  - application/json

paths:
  /quentinoo:
    get:
      summary: Gtet Quientin
      operationId: add_update_employee
      responses:
       '200':
          description: OK
          schema:
            type: string
      x-google-backend:
        address: https://timelime-backend-uavkhjofda-nw.a.run.app/quentin
        protocol: h2
      security:
        - bearer: []

securityDefinitions:
  bearer:
    authorizationUrl: ""
    flow: "implicit"
    type: "oauth2"
    x-google-issuer: "main-641@timelime-dev.iam.gserviceaccount.com"
    x-google-jwks_uri: "https://www.googleapis.com/robot/v1/metadata/x509/main-641@timelime-dev.iam.gserviceaccount.com"
    x-google-jwt-locations:
      - header: "Authorization"
        value_prefix: "Bearer 